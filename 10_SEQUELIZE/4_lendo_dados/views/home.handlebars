<h1>Usuarios Cadastrados:</h1>
<div class="container">
    {{#each users}}
    <div class="item">
        <h2>{{this.name}}</h2>
        <p>{{this.occupation}}</p>
        <div class="actio">
            <a href="/users/{{this.id}}">ver detalhes</a>
        </div>
    </div>
    {{/each}}
</div>